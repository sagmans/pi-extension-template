# 2026-08-28 Tooling, SDK Audit, and Publishing Lifecycle

## Purpose

This migration makes mise authoritative, expands report-only Pi SDK conformance auditing, and replaces fragmented release notes with a complete package publishing lifecycle.

## Required changes

1. Replace `.nvmrc` and duplicated workflow runtime versions with root `mise.toml` pinning Node `24.20.0`.
2. Use the SHA-pinned mise action in CI and release workflows. Keep release caches disabled.
3. Extend `audit-pi-extension` to inspect official public SDK use, private or internal imports, monkey patches, prototype/global mutation, undocumented runtime coupling, unsupported host assumptions, and public-API alternatives. Report only; take no corrective action.
4. Adopt the numbered package publishing lifecycle covering Day 0, one-time manual new-package bootstrap with interactive two-factor authentication, existing-package onboarding, npm/GitHub OIDC setup, recurring releases, maintenance, and recovery.
5. Preserve stronger target-specific controls and feature-level real usage.

## Validation

Run:

```bash
mise install
mise exec -- npm ci --ignore-scripts
mise exec -- npm run verify:ci
```

Audit one representative extension without changes. Confirm CI obtains Node from `mise.toml`. For publishing targets, compare npm trusted-publisher values with GitHub owner, repository, `release.yml`, and `npm-release` before the next signed tag.

Advance `.pi-extension-template.json` to `2026-08-28` only after all applicable changes and target-specific verification pass.
